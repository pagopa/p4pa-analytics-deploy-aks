microservice-chart:
  image:
    repository: ghcr.io/pagopa/p4pa-analytics-data-ingestion
    tag: v1.0.0@sha256:5101bf42f30bdb2b059e00711c5cfa7c82f785cb3bcc027301759edb24419d52
    pullPolicy: Always

  ingress:
    host: "analytics.internal.p4pa.pagopa.it"

  resources:
    requests:
      memory: "512Mi"
      cpu: "40m"
    limits:
      memory: "1536Mi"
      cpu: "1500m"

  autoscaling:
    enable: false

  envConfig:
    ENV: "PROD"
    JAVA_TOOL_OPTIONS: >-
      -Djava.util.concurrent.ForkJoinPool.common.parallelism=4
      -Djdk.virtualThreadScheduler.parallelism=4
      -Djdk.virtualThreadScheduler.maxPoolSize=256
      -XX:InitialRAMPercentage=75.0
      -XX:MaxRAMPercentage=85.0
      -XX:+UseG1GC
      -XX:MaxGCPauseMillis=200
      -XX:ParallelGCThreads=4
      -XX:ConcGCThreads=1
      -XX:MinTLABSize=16k
      -XX:+ResizeTLAB
      -XX:+UseContainerSupport
      -XX:+ExitOnOutOfMemoryError
      -XX:+PerfDisableSharedMem
      -XX:+AlwaysPreTouch
      -XX:+UseTransparentHugePages
      -XX:NativeMemoryTracking=detail
      -Djdk.nio.maxCachedBufferSize=262144
      -Djava.security.egd=file:/dev/./urandom
      -Dspring.profiles.active=prod
      -Djava.rmi.server.hostname=127.0.0.1
      -javaagent:/app/applicationinsights-agent.jar
      -Dapplicationinsights.configuration.file=/mnt/file-config-external/appinsights-config/applicationinsights.json

    TEMPORAL_SERVER_ENABLE_HTTPS: "true"
    TEMPORAL_SERVER_HOST: "api.temporal.internal.p4pa.pagopa.it"
    TEMPORAL_SERVER_PORT: "443"

  keyvault:
    name: "p4pa-p-payhub-kv"
    tenantId: "7788edaf-0346-4068-9d79-c868aed15b3d"
